# Optional analysis / nowcasting dependencies (WT6).
# Install with: pip install -r requirements-analysis.txt
# Base requirements.txt is owned by WT7 and is left unchanged.
numpy>=1.26.0
scipy>=1.11.0
Pillow>=10.0.0
